<div class="container top-space">
  <h1 class="text-center">Business Donations</h1>
  <% @bizs.each do |biz| %>
    <div clas='row'> 
      <div class='col-md-3'> 
        <h2><%= biz.business_name %></h2>
        <%= image_tag(biz.logo.url(:medium)) %>
      </div>
    </div>
  <% end %>
</div>
